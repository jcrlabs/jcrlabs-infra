---
# Host donde corre Docker + k3d
all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
    ansible_python_interpreter: /usr/bin/python3

  children:
    k3d_host:
      hosts:
        homelab:
          ansible_host: 192.168.1.10   # IP real del host
