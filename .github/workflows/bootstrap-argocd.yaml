name: "Bootstrap: Install ArgoCD"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Escribe INSTALL para confirmar"
        required: true

jobs:
  healthcheck:
    runs-on: ubuntu-latest
    steps:
      - run: |
          HTTP_CODE=$(curl -sL -o /dev/null -w "%{http_code}" \
            --max-time 15 --retry 3 --retry-delay 5 \
            https://tallerjcr.jcrlabs.net)
          if [ "$HTTP_CODE" -lt 200 ] || [ "$HTTP_CODE" -ge 500 ]; then
            echo "::error::tallerjcr.jcrlabs.net no responde. Abortar."
            exit 1
          fi
          echo "OK: cluster activo (HTTP $HTTP_CODE)"

  install:
    runs-on: [self-hosted, homelab]
    needs: healthcheck
    steps:
      - name: Validar confirmacion
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "INSTALL" ]; then
            echo "::error::Escribe INSTALL para confirmar"
            exit 1
          fi

      - uses: actions/checkout@v4

      - name: Add helm repos
        run: |
          helm repo add argo https://argoproj.github.io/argo-helm
          helm repo add sealed-secrets https://bitnami-labs.github.io/sealed-secrets
          helm repo update

      - name: Instalar Sealed Secrets
        run: |
          helm upgrade --install sealed-secrets sealed-secrets/sealed-secrets \
            --namespace sealed-secrets --create-namespace \
            --set fullnameOverride=sealed-secrets-controller \
            --atomic --timeout 3m

      - name: Instalar ArgoCD
        run: |
          helm upgrade --install argocd argo/argo-cd \
            --namespace argocd --create-namespace \
            --set configs.params.server\.insecure=true \
            --set server.ingress.enabled=true \
            --set server.ingress.ingressClassName=nginx \
            --set 'server.ingress.annotations.cert-manager\.io/cluster-issuer=letsencrypt-cloudflare' \
            --set 'server.ingress.annotations.nginx\.ingress\.kubernetes\.io/ssl-redirect=false' \
            --set 'server.ingress.annotations.nginx\.ingress\.kubernetes\.io/force-ssl-redirect=false' \
            --set 'server.ingress.hosts[0]=argocd.jcrlabs.net' \
            --set 'server.ingress.tls[0].secretName=argocd-tls' \
            --set 'server.ingress.tls[0].hosts[0]=argocd.jcrlabs.net' \
            --set server.ingressGrpc.enabled=false \
            --atomic --timeout 5m

      # Garantiza que el pod lee server.insecure=true del ConfigMap
      # El helm --set a veces no se refleja hasta el siguiente restart
      - name: Forzar insecure mode en ConfigMap y reiniciar
        run: |
          kubectl patch configmap argocd-cmd-params-cm -n argocd \
            --type=merge \
            -p='{"data":{"server.insecure":"true"}}'
          kubectl rollout restart deployment argocd-server -n argocd
          kubectl rollout status deployment argocd-server -n argocd --timeout=120s

      - name: Aplicar AppProject y ApplicationSets
        run: kubectl apply -k argocd/

      - name: Password inicial ArgoCD
        run: |
          PASS=$(kubectl -n argocd get secret argocd-initial-admin-secret \
            -o jsonpath="{.data.password}" | base64 -d)
          echo "URL : https://argocd.jcrlabs.net"
          echo "User: admin"
          echo "Pass: $PASS"

      - name: Health check final
        run: |
          HTTP_CODE=$(curl -sL -o /dev/null -w "%{http_code}" \
            --max-time 15 --retry 3 --retry-delay 5 \
            https://tallerjcr.jcrlabs.net)
          if [ "$HTTP_CODE" -lt 200 ] || [ "$HTTP_CODE" -ge 500 ]; then
            echo "::error::tallerjcr.jcrlabs.net NO responde tras instalar ArgoCD"
            exit 1
          fi
          echo "OK: tallerjcr.jcrlabs.net sigue activo"
