apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: portfolio-apps-prod
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/jcrlabs/jcrlabs-infra.git
        revision: HEAD
        directories:
          - path: "k8s/apps/*/"
          # taller-inventario se gestiona manualmente (tallerjcr.jcrlabs.net)
          - path: "k8s/apps/taller-inventario/"
            exclude: true
  template:
    metadata:
      name: "{{ .path.basename }}"
      labels:
        env: production
        project: "{{ .path.basename }}"
    spec:
      project: portfolio
      source:
        repoURL: https://github.com/jcrlabs/jcrlabs-infra.git
        targetRevision: HEAD
        path: "{{ .path.path }}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .path.basename }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        retry:
          limit: 3
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
