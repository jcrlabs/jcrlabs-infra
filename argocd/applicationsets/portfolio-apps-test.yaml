apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: portfolio-apps-test
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/jonathanCaamano/jcrlabs-infra.git
        revision: HEAD
        directories:
          - path: "k8s/apps-test/*/"
          # inventory-test excluido hasta migracion a GitOps
          - path: "k8s/apps-test/inventory/"
            exclude: true
  template:
    metadata:
      name: "{{ .path.basename }}-test"
      labels:
        env: test
        project: "{{ .path.basename }}"
    spec:
      project: portfolio
      source:
        repoURL: https://github.com/jonathanCaamano/jcrlabs-infra.git
        targetRevision: HEAD
        path: "{{ .path.path }}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .path.basename }}-test"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        retry:
          limit: 3
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
