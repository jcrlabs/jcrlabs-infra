apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # El namespace `nextcloud` lo crea ArgoCD con CreateNamespace=true.
  # (k8s/namespaces/nextcloud.yaml es solo registro declarativo, como el resto.)
  - storage.yaml
